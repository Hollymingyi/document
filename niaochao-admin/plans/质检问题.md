# 质检问题
## 质检-规则库 规则库列表，offline-qc/rule-library 点击「引用计数」，未展示引用该规则的评分卡里的评分项 

## 质检-评分卡 新建、编辑评分卡-新增评分项，offline-qc/scorecard/edit/10 先选中多个规则，然后去搜索框输入规则并选中搜索结果的规则后，删除搜索框里的规则后，原来选中的规则被取消选中

## 质检-评分卡 offline-qc/scorecard/edit/10 单声道需识别隐藏掉，功能暂时不用（已对接阿里的身份识别模型处理该模块）

## 质检-评分卡 进入评分卡详情-评分项编排，offline-qc/scorecard/detail/12 点击关联规则名称，不做跳转，文字按钮修改为文本展示 
    
## 质检-评分卡 同一个评分卡里的不同评分项中，不可以维护同一个规则

## 质检-任务 offline-qc/task-list 点击重跑后，原任务的录音、指标信息都被清空了（应保持原纪录信息不变，只对新任务做重跑），这个不同于评分卡规则重跑逻辑（这个保持不变）， 

## 质检-任务列表 offline-qc/task-list 点击重跑，弹窗中数据源选择「仅失败」后，重跑深度应不支持选择「仅重跑规则」，将「仅重跑规则」同步置disable

## 质检-任务列表 offline-qc/task-list  任务列表点击重跑产生新任务后，再次重跑，弹窗中的提交按钮无法点击（一个任务可以重跑几次 ，不限制，由业务运营人为把控，系统不拦截）

## 质检-任务详情 offline-qc/task-list 进入重跑过的任务详情，点击「查看原任务」后 应跳转到原任务详情。

## 质检-任务详情 进入任务详情-评分卡下方的命中结果，点击「展开复核」，复核详情应展示在复核项的下面（当前是统一展示在最底部，如果多个复核的话不好看）

## 质检-任务详情 offline-qc/record-detail/rec_5589f025b564413a9c3eb987 进入任务详情点击查看修正记录，
###1.记录中「修正时间」未展示；
###2.「操作类型」应展示中文枚举；
###3.「详情」中的字段应展示字段中文名

## 质检-任务详情 进入任务详情-复核明细，把复核结果都改为「未命中」后，点击增加命中并配置触发文本，
###1.触发文本重复展示；
###2.手动添加的触发文本 命中的对话 未展示在左侧的对话详情中

#注意事项
##用playwright-cli 测试，用户名admin 密码Gt123456 用apiLogin绕过验证码（ocr识别不准），要修正问题并测试成功后才结束